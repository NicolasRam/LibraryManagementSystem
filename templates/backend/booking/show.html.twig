{% extends "view_layout.html.twig" %}

{% block body %}
    <h1>Booking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ booking.id }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ booking.startDate ? booking.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ booking.endDate ? booking.endDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ReturnDate</th>
                <td>{{ booking.returnDate ? booking.returnDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_booking_index') }}">back to list</a>

    <a href="{{ path('backend_booking_edit', {'id': booking.id}) }}">edit</a>

    {{ include('backend/booking/_delete_form.html.twig') }}
{% endblock %}